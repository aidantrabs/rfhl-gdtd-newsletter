

---


Build Issue #1 of the Digital Products newsletter — "Meet Digital Products". Output a single self-contained HTML file at issues/issue-1.html. Everything (HTML, CSS, JS) must be in that one file. No external dependencies, no CDN links, no Google Fonts — system fonts only. It must work when dragged into a browser offline.

The design must match the dark, immersive, interactive style of the reference file — dark navy background, accent colours per section, scroll-triggered fade-in animations, gradient headers, hover effects on cards, and a sticky nav. This is not a corporate memo — it's an interactive magazine.

Every section needs interactivity. Expandable org chart cards. Tabbed content. Hover-to-reveal product cards. An animated counter row that counts up when scrolled into view. A poll at the end. Scroll-triggered reveals on every section. At least one easter egg hidden somewhere.

After the main page, also build the Outlook-compatible email teaser at email/issue-1-teaser.html — table-based HTML, all styles inlined, under 100KB. Its only job is to look sharp and get one click through to the full page.

Use [PLACEHOLDER] comments for any content that needs to be swapped with real names, photos, or links. Use realistic placeholder content everywhere else — write it as if you're actually introducing a real department to people who know nothing about it.

Test at 375px mobile width — everything must work and look good on phones.

Start by reading both files, then build.
